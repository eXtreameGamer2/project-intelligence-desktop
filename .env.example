# Desktop builds always use local SQLite. Do not set DATABASE_URL or Supabase keys.

PORT=4310
HOST=127.0.0.1
SERVE_STATIC=true

# Optional: point Overview/Dashboard AI at a local model such as LM Studio.
# Configure that in the in-app Settings page (localhost provider).
